const colours = ["green", "red", "rgba(133, 122, 200)", "#f15025"];

const btn = document.getElementById('btn');
const colour = document.querrySelector('.colour');

btn.addEventListener('click', function() {
    
    const randomNumber = 2;
})